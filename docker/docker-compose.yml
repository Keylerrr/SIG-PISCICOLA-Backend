services:
  web:
    build:
      context: ../
      dockerfile: docker/Dockerfile
    restart: always
    ports:
      - "8000:8000"
    environment:
      DJANGO_SETTINGS_MODULE: configs.dev_settings
    volumes:
      - ../:/app
